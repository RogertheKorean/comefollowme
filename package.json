{
  "name": "together-insights",
  "version": "4.0.0",
  "private": true,
  "description": "Bilingual weekly study, shared reflections, email and guest participation with Supabase.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
