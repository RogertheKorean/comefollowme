{
  "name": "together-insights",
  "version": "4.5.0",
  "private": true,
  "description": "Bilingual weekly study with immediate anonymous participation and assigned operator access.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.cjs",
    "start": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
